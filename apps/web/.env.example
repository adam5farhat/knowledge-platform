# Base URL of the API as seen from the browser. Must match the API’s PUBLIC_API_URL (scheme + host, no trailing slash).
# Production example: https://api.example.com
NEXT_PUBLIC_API_URL=http://localhost:3001
